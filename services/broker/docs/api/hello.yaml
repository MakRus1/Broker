openapi: 3.0.3
info:
  title: broker hello API
  version: '1.0'

paths:
  /hello:
    get:
      operationId: helloGet
      parameters:
        - name: name
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
    post:
      operationId: helloPost
      parameters:
        - name: name
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
